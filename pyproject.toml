[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "oracleofkalshi"
version = "0.1.0"
dependencies = [
    'requests',
    'kalshi_python_sync',
    'torch'
]
requires-python = '>= 3.12.3'